<?xml version="1.0" encoding="UTF-8"?>

<gateway-config xmlns="http://xmlns.kaazing.com/2012/09/gateway">

  <properties>
    <property>
      <name>proxy.to.logical.hostname</name>
      <value>cloud</value>
    </property>
    <property>
      <name>proxy.to.logical.port</name>
      <value>2375</value>
    </property>
    <property>
      <name>reverse.connection.hostname</name>
      <value>cloud</value>
    </property>
    <property>
      <name>reverse.connection.port</name>
      <value>2376</value>
    </property>
  </properties>

  <service>

    <name>KWIC PROXY</name>
    <description>
        Accepts reverse connection on ${reverse.connection.hostname}:${reverse.connection.port} to create a
        proxy connection from ${proxy.to.logical.hostname}:${proxy.to.logical.port} to an internal service
    </description>

    <accept>tcp://${proxy.to.logical.hostname}:${proxy.to.logical.port}</accept>

    <connect>tcp://${proxy.to.logical.hostname}:${proxy.to.logical.port}</connect>

    <type>proxy</type>

    <connect-options>
        <tcp.transport>socks://${reverse.connection.hostname}:${reverse.connection.port}</tcp.transport>
        <socks.mode>reverse</socks.mode>
        <socks.timeout>2 seconds</socks.timeout>
        <socks.transport>wsn://${reverse.connection.hostname}:${reverse.connection.port}/kwic1</socks.transport>
        <ws.inactivity.timeout>60 seconds</ws.inactivity.timeout>
    </connect-options>

    <cross-site-constraint>
      <allow-origin>*</allow-origin>
    </cross-site-constraint>

  </service>

</gateway-config>


